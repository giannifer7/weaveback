¤h1(weaveback-api)

Shared business logic for the weaveback toolchain.

No HTTP, no LLM, no subprocess calls — pure query and analysis
functions that can be called from any binary in the workspace,
from Python via PyO3, and from MCP tool handlers.

¤h2(Modules)

¤rust_chunk(weaveback-api-lib, ¤[
pub mod apply_back;
pub mod coverage;
pub mod lint;
pub mod lsp_runner;
pub mod lookup;
pub mod mcp;
pub mod process;
pub mod query;
pub mod semantic;
pub mod tag;
pub mod tangle;
¤])

¤h2(Assembly)

¤rust_chunk(@file weaveback-api/src/lib.rs, ¤[
// <[weaveback-api-lib]>
¤])
