¤h1(¤[Serve Test Assembly¤])

The generated `tests.rs` file includes focused generated test bodies so runtime
and test concerns stay physically separate.

¤rust_file(weaveback-serve/src/tests.rs, ¤[
mod helpers;
use helpers::*;
mod core;
mod edge_cases;
mod editing;
¤])

¤rust_file(weaveback-serve/src/tests/helpers.rs, ¤[
// <[serve-tests-helpers]>
¤])

¤rust_file(weaveback-serve/src/tests/core.rs, ¤[
use super::*;

// <[serve-tests-core]>
¤])

¤rust_file(weaveback-serve/src/tests/edge_cases.rs, ¤[
use super::*;

// <[serve-tests-edge-cases]>
¤])

¤rust_file(weaveback-serve/src/tests/editing.rs, ¤[
use super::*;

// <[serve-tests-editing]>
¤])
