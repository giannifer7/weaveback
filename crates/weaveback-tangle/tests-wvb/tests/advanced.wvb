¤h1(¤[Advanced Tangle Tests¤])

The advanced test module is intentionally a thin map. Each child module owns one behavior family so regressions can be reviewed without scanning a monolithic generated file.

¤redef(rust_chunk, name, body, ¤{¤code_block(rust, ¤{
// <[¤(name)]>=¤(body)// @@
¤})¤})

¤redef(rust_file, path, body, ¤{¤rust_chunk(@file ¤(path), ¤{
// ¤(path)
// ¤pastafarian_warning()
¤(body)
¤})¤})

¤rust_file(weaveback-tangle/src/tests/advanced.rs, ¤[
mod chunks;
mod replace;
mod modifiers;
mod paths;
mod unused;
mod tangle_check;
mod syntax;
mod outputs;
mod strict_write;
¤])
