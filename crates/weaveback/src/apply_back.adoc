= Apply-Back (re-export)
:toc: left
:source-highlighter: syntect

Re-exports `weaveback-api::apply_back` so the combined `weaveback` binary
can continue using `apply_back::run_apply_back`, `ApplyBackOptions`, and
`ApplyBackError` without call-site changes.

[source,rust]
----
// <<apply-back-reexport>>=
pub use weaveback_api::apply_back::*;
// @
----

[source,rust]
----
// <<@file weaveback/src/apply_back.rs>>=
// <<apply-back-reexport>>
// @
----
