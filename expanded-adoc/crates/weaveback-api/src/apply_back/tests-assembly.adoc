= Apply-Back Test Assembly

The generated test module is assembled from focused modules so each concern can be reviewed independently.

[source,rust]
----
// <[@file weaveback-api/src/apply_back/tests.rs]>=
// weaveback-api/src/apply_back/tests.rs
// I'd Really Rather You Didn't edit this generated file.

use super::*;
use std::fs;
use weaveback_tangle::db::{ChunkDefEntry, Confidence};

fn lines(s: &str) -> Vec<String> {
    s.lines().map(str::to_string).collect()
}

struct TestWorkspace {
    root: std::path::PathBuf,
}
impl TestWorkspace {
    fn new() -> Self {
        let unique = format!(
            "wb-apply-back-tests-{}-{}",
            std::process::id(),
            std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH).unwrap().as_nanos()
        );
        let root = std::env::temp_dir().join(unique);
        std::fs::create_dir_all(&root).unwrap();
        Self { root }
    }
    fn write_file(&self, rel: &str, content: &[u8]) {
        let path = self.root.join(rel);
        if let Some(p) = path.parent() {
            std::fs::create_dir_all(p).unwrap();
        }
        std::fs::write(path, content).unwrap();
    }
    fn open_db(&self) -> WeavebackDb {
        WeavebackDb::open(self.root.join("weaveback.db")).unwrap()
    }
}
impl Drop for TestWorkspace {
    fn drop(&mut self) {
        let _ = std::fs::remove_dir_all(&self.root);
    }
}


mod apply_file;
mod batch;
mod primitives;
mod resolution;
mod runner;
mod workspace;

// @
----


[source,rust]
----
// <[@file weaveback-api/src/apply_back/tests/primitives.rs]>=
// weaveback-api/src/apply_back/tests/primitives.rs
// I'd Really Rather You Didn't edit this generated file.

use super::*;

// <[applyback-tests-primitives]>

// @
----


[source,rust]
----
// <[@file weaveback-api/src/apply_back/tests/workspace.rs]>=
// weaveback-api/src/apply_back/tests/workspace.rs
// I'd Really Rather You Didn't edit this generated file.

use super::*;

// <[applyback-tests-workspace]>

// @
----


[source,rust]
----
// <[@file weaveback-api/src/apply_back/tests/resolution.rs]>=
// weaveback-api/src/apply_back/tests/resolution.rs
// I'd Really Rather You Didn't edit this generated file.

use super::*;

// <[applyback-tests-resolution]>

// @
----


[source,rust]
----
// <[@file weaveback-api/src/apply_back/tests/apply_file.rs]>=
// weaveback-api/src/apply_back/tests/apply_file.rs
// I'd Really Rather You Didn't edit this generated file.

use super::*;

// <[applyback-tests-apply-file]>

// @
----


[source,rust]
----
// <[@file weaveback-api/src/apply_back/tests/runner.rs]>=
// weaveback-api/src/apply_back/tests/runner.rs
// I'd Really Rather You Didn't edit this generated file.

use super::*;

// <[applyback-tests-runner]>

// @
----


[source,rust]
----
// <[@file weaveback-api/src/apply_back/tests/batch.rs]>=
// weaveback-api/src/apply_back/tests/batch.rs
// I'd Really Rather You Didn't edit this generated file.

use super::*;

// <[applyback-tests-batch]>

// @
----

